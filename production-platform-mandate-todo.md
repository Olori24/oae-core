# OAE Production Platform Mandate

- [ ] Review the complete attached mandate and identify the requested outcomes, prohibited shortcuts, and claims requiring measurement.
- [x] Establish a reproducible repository baseline using the existing quality, security, and coverage gates. On commit `dcc50cf`, pytest reported 840 passed and 4 skipped; coverage was 82.13% against the required 70%; Ruff passed; and mypy reported no issues across 323 source files. The run emitted one FastAPI TestClient deprecation warning and no quality-gate failures.
- [x] Audit tenant controls, identity, repositories, workspaces, durable jobs, event delivery, APIs, data access, deployment, and observability against implementation evidence. Findings are recorded in `docs/PRODUCTION_PLATFORM_MANDATE_BASELINE.md` and distinguish real PostgreSQL-backed controls from partial, mocked, unmeasured, and real-host-only claims.
- [x] Produce a risk-ranked roadmap that separates code work from real-host and scale validation requirements. The first implementation priority is a durable, tenant-scoped authorization boundary enforced by workers before execution.
- [x] Implement only prioritized, test-backed changes after the evidence review is complete. Added the PostgreSQL worker-authorization foundation, tenant-scoped pending approval requests, durable audit events, a no-self-approval repository API boundary, and optional worker-side enforcement for build operations.
- [x] Run comprehensive validation for the delivered authorization foundation. The full suite reported 845 passed and 4 skipped; coverage was 81.89% against the 70% threshold; Ruff passed; mypy found no issues across 324 source files; and `git diff --check` passed. PostgreSQL integration tests remain skipped unless `OAE_POSTGRES_TEST_URL` is supplied.
- [x] Publish an evidence-based production-readiness report with remaining constraints and next steps. The report is `docs/PRODUCTION_PLATFORM_MANDATE_BASELINE.md`; it documents the verified baseline, the new authorization foundation, and the real-host, scale, backup, and commercial-operation proof that remains outstanding.
